package com.example

class HelloServerlet { }

/**
 *
 * 1. serverlet:生成一个WAR，部署到servlet的webapp目录下，即可生效
 *      需要部署到serverlet容器当中，servlet处理的是path，端口，连接等等由部署到的servlet容器负责(Tomcat等)
 *
 *  2.将Serverlet容器打包进代码：生成一个JAR
 *
 */